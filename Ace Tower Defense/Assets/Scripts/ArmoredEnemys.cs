using UnityEngine;

public class ArmoredEnemys : EnemyBasklass
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        isArmored = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
